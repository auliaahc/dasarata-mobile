name: CI - Build APK

on:
  pull_request:
    branches:
      - main
      - development

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-java@v1
      with:
        java-version: '17.x'
    - uses: subosito/flutter-action@v1
      with:
        channel: 'beta'
    - run: flutter pub get
    # - run: dart format --set-exit-if-changed .
    # - run: flutter analyze .
    - name: Create google_maps_api.xml
      run: |
        mkdir -p android/app/src/main/res/values/
        echo '<?xml version="1.0" encoding="utf-8"?>' > android/app/src/main/res/values/google_maps_api.xml
        echo '<resources>' >> android/app/src/main/res/values/google_maps_api.xml
        echo '    <string name="GoogleMapsKey">${{ secrets.GOOGLE_MAPS_API_KEY }}</string>' >> android/app/src/main/res/values/google_maps_api.xml
        echo '</resources>' >> android/app/src/main/res/values/google_maps_api.xml
    - name: Setup .env file
      run: |
        echo "BASE_URL=${{ secrets.BASE_URL }}" >> .env
        echo "BASE_URL_GOOGLE_MAPS=${{ secrets.BASE_URL_GOOGLE_MAPS }}" >> .env
        echo "GOOGLE_MAPS_API_KEY=${{ secrets.GOOGLE_MAPS_API_KEY }}" >> .env
    - name: Generate env.g.dart
      run: flutter pub run build_runner build --delete-conflicting-outputs
    - run: flutter build apk
    - uses: actions/upload-artifact@v3
      with:
        name: release-apk
        path: build/app/outputs/apk/release/app-release.apk
